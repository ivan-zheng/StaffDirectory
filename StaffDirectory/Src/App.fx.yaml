App As appinfo:
    BackEnabled: =false
    StartScreen: =Set(varUserName, User().Name);

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

